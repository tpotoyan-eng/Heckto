export interface Product {
  name: string;
  currentPrice: number;
  originalPrice: number;
  rating: number;
  description: string;
  url: string;
}

export interface discountProduct {
  smallTitle: string;
  Title: string;
  paragraph: string;
  _id: number;
  imgUrl: string;
}
